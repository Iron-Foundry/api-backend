# Stored document shapes are built inline in routers/events.py using the
# dataclasses returned by services/parser.py.
#
# This module is reserved for any shared response/query models added later
# (e.g. paginated loot leaderboard responses, member activity summaries).
